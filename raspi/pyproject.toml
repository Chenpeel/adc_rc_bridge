[project]
name = "raspi-bridge"
version = "0.1.0"
description = "Raspberry Pi I2C to WebSocket bridge"
requires-python = ">=3.11"
dependencies = [
  "smbus2>=0.4.3",
  "websockets>=12.0",
]

[tool.uv]
package = false
